service: mahnoor-temp-hvac-test-MAY9

frameworkVersion: "3"

provider:
  name: aws
  runtime: python3.11
  region: us-east-2
  stage: ${opt:stage, 'dev'}

  environment:
    DYNAMODB_TABLE: mahnoor-temp-hvac-table
    SQS_QUEUE_URL: !Ref mahnoorTempHvacQueue
    IOT_TOPIC: mahnoor-temp/hvac/data
    STAGE: ${self:provider.stage}

  iam:
    role:
      name: mahnoor-temp-hvac-lambda-role-may9
      statements:
        - Effect: Allow
          Action:
            - dynamodb:PutItem
            - dynamodb:GetItem
            - dynamodb:Scan
            - dynamodb:Query
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource:
            - !GetAtt mahnoorTempHvacTable.Arn
            - !Sub "${mahnoorTempHvacTable.Arn}/index/*"
        - Effect: Allow
          Action:
            - sqs:SendMessage
            - sqs:ReceiveMessage
            - sqs:DeleteMessage
            - sqs:GetQueueAttributes
          Resource: !GetAtt mahnoorTempHvacQueue.Arn
        - Effect: Allow
          Action:
            - iot:Publish
            - iot:Subscribe
            - iot:Connect
            - iot:Receive
          Resource: "*"
        # MQTT.fx provisioner — certificate + attachment permissions
        - Effect: Allow
          Action:
            - iot:CreateKeysAndCertificate
            - iot:DeleteCertificate
            - iot:UpdateCertificate
            - iot:AttachPolicy
            - iot:DetachPolicy
            - iot:AttachThingPrincipal
            - iot:DetachThingPrincipal
            - iot:DescribeEndpoint
          Resource: "*"
        - Effect: Allow
          Action:
            - ssm:PutParameter
            - ssm:DeleteParameter
            - ssm:GetParameter
          Resource: !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/mahnoor/mqttfx/*"
        - Effect: Allow
          Action:
            - lambda:InvokeFunction
          Resource: "*"
        - Effect: Allow
          Action:
            - logs:CreateLogGroup
            - logs:CreateLogStream
            - logs:PutLogEvents
          Resource: "*"

functions:
  # Lambda 1: Triggered by SQS, saves HVAC data to DynamoDB
  mahnoorTempIngestHvac:
    name: mahnoor-temp-ingest-hvac
    handler: ingest_hvac.handler
    description: "Receives HVAC data from SQS and saves to DynamoDB"
    timeout: 30
    memorySize: 256
    events:
      - sqs:
          arn: !GetAtt mahnoorTempHvacQueue.Arn
          batchSize: 10
          functionResponseType: ReportBatchItemFailures
    environment:
      PROCESSOR_LAMBDA_NAME: mahnoor-temp-process-hvac

  # Lambda 2: Triggered after DynamoDB save, processes/alerts
  mahnoorTempProcessHvac:
    name: mahnoor-temp-process-hvac
    handler: process_hvac.handler
    description: "Processes saved HVAC data, triggers alerts if needed"
    timeout: 30
    memorySize: 256
    events:
      - stream:
          type: dynamodb
          arn: !GetAtt mahnoorTempHvacTable.StreamArn
          startingPosition: LATEST
          batchSize: 10
          filterPatterns:
            - eventName: [INSERT, MODIFY]

  # Lambda 3: API Gateway backed Lambda for dashboard
  mahnoorTempDashboardApi:
    name: mahnoor-temp-dashboard-api
    handler: dashboard_api.handler
    description: "REST API for HVAC dashboard"
    timeout: 30
    memorySize: 256
    events:
      - http:
          path: /hvac
          method: get
          cors: true
      - http:
          path: /hvac/{deviceId}
          method: get
          cors: true
      - http:
          path: /hvac/stats
          method: get
          cors: true
      - http:
          path: /hvac/latest
          method: get
          cors: true

  # Lambda 4: Custom Resource — provisions IoT cert for MQTT.fx during deploy
  mahnoorMqttFxProvisioner:
    name: mahnoor-mqttfx-provisioner
    handler: mqttfx_provisioner.handler
    description: "Custom resource — creates IoT X.509 cert for MQTT.fx, stores keys in SSM"
    timeout: 60
    memorySize: 128

resources:
  Resources:
    # ─────────────────────────────────────────
    # SQS Queue (with Dead Letter Queue)
    # ─────────────────────────────────────────
    mahnoorTempHvacDLQ:
      Type: AWS::SQS::Queue
      Properties:
        QueueName: mahnoor-temp-hvac-dlq
        MessageRetentionPeriod: 1209600  # 14 days
        Tags:
          - Key: Project
            Value: mahnoor-temp
          - Key: Component
            Value: hvac-pipeline

    mahnoorTempHvacQueue:
      Type: AWS::SQS::Queue
      Properties:
        QueueName: mahnoor-temp-hvac-queue
        VisibilityTimeout: 60
        MessageRetentionPeriod: 86400  # 1 day
        RedrivePolicy:
          deadLetterTargetArn: !GetAtt mahnoorTempHvacDLQ.Arn
          maxReceiveCount: 3
        Tags:
          - Key: Project
            Value: mahnoor-temp
          - Key: Component
            Value: hvac-pipeline

    # ─────────────────────────────────────────
    # SQS Queue Policy (allows IoT to send)
    # ─────────────────────────────────────────
    mahnoorTempHvacQueuePolicy:
      Type: AWS::SQS::QueuePolicy
      Properties:
        Queues:
          - !Ref mahnoorTempHvacQueue
        PolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Principal:
                Service: iot.amazonaws.com
              Action: sqs:SendMessage
              Resource: !GetAtt mahnoorTempHvacQueue.Arn

    # ─────────────────────────────────────────
    # DynamoDB Table
    # ─────────────────────────────────────────
    mahnoorTempHvacTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: mahnoor-temp-hvac-table
        BillingMode: PAY_PER_REQUEST
        StreamSpecification:
          StreamViewType: NEW_AND_OLD_IMAGES
        AttributeDefinitions:
          - AttributeName: deviceId
            AttributeType: S
          - AttributeName: timestamp
            AttributeType: S
        KeySchema:
          - AttributeName: deviceId
            KeyType: HASH
          - AttributeName: timestamp
            KeyType: RANGE
        GlobalSecondaryIndexes:
          - IndexName: mahnoor-temp-timestamp-index
            KeySchema:
              - AttributeName: timestamp
                KeyType: HASH
            Projection:
              ProjectionType: ALL
        TimeToLiveSpecification:
          AttributeName: ttl
          Enabled: true
        Tags:
          - Key: Project
            Value: mahnoor-temp
          - Key: Component
            Value: hvac-pipeline

    # ─────────────────────────────────────────
    # IoT Rule → SQS
    # ─────────────────────────────────────────
    mahnoorTempIotRole:
      Type: AWS::IAM::Role
      Properties:
        RoleName: mahnoor-temp-iot-role-may9
        AssumeRolePolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Principal:
                Service: iot.amazonaws.com
              Action: sts:AssumeRole
        Policies:
          - PolicyName: mahnoor-temp-iot-sqs-policy
            PolicyDocument:
              Version: "2012-10-17"
              Statement:
                - Effect: Allow
                  Action: sqs:SendMessage
                  Resource: !GetAtt mahnoorTempHvacQueue.Arn

    mahnoorTempIotRule:
      Type: AWS::IoT::TopicRule
      Properties:
        RuleName: mahnoor_temp_hvac_rule
        TopicRulePayload:
          Description: "Routes HVAC sensor data from IoT to SQS"
          Sql: "SELECT * FROM 'mahnoor-temp/hvac/data'"
          RuleDisabled: false
          Actions:
            - Sqs:
                QueueUrl: !Ref mahnoorTempHvacQueue
                RoleArn: !GetAtt mahnoorTempIotRole.Arn
                UseBase64: false

    # ─────────────────────────────────────────
    # MQTT.fx Monitoring — Thing, Policy, Certificate (Custom Resource)
    # ─────────────────────────────────────────
    mahnoorMqttFxThing:
      Type: AWS::IoT::Thing
      Properties:
        ThingName: mahnoor-mqttfx-monitor
        AttributePayload:
          Attributes:
            purpose: monitoring
            tool: mqttfx

    mahnoorMqttFxPolicy:
      Type: AWS::IoT::Policy
      Properties:
        PolicyName: mahnoor-mqttfx-monitor-policy
        PolicyDocument:
          Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Action:
                - iot:Connect
                - iot:Subscribe
                - iot:Receive
                - iot:Publish
              Resource:
                - !Sub "arn:aws:iot:${AWS::Region}:${AWS::AccountId}:client/mahnoor-mqttfx-monitor"
                - !Sub "arn:aws:iot:${AWS::Region}:${AWS::AccountId}:topic/mahnoor-temp/*"
                - !Sub "arn:aws:iot:${AWS::Region}:${AWS::AccountId}:topicfilter/mahnoor-temp/*"

    # Custom::IoTCertificate invokes mahnoorMqttFxProvisioner Lambda during deploy.
    # On CREATE: generates cert+key, attaches policy+thing, stores in SSM.
    # On DELETE: detaches, deactivates, deletes cert, cleans SSM.
    mahnoorMqttFxCertificate:
      Type: Custom::IoTCertificate
      DependsOn:
        - mahnoorMqttFxThing
        - mahnoorMqttFxPolicy
      Properties:
        ServiceToken: !GetAtt MahnoorMqttFxProvisionerLambdaFunction.Arn
        ThingName: mahnoor-mqttfx-monitor
        PolicyName: mahnoor-mqttfx-monitor-policy
        Region: !Ref AWS::Region

  Outputs:
    IoTTopicName:
      Description: "Publish HVAC data to this IoT topic"
      Value: "mahnoor-temp/hvac/data"

    SQSQueueUrl:
      Description: "SQS Queue URL"
      Value: !Ref mahnoorTempHvacQueue

    SQSQueueArn:
      Description: "SQS Queue ARN"
      Value: !GetAtt mahnoorTempHvacQueue.Arn

    DynamoDBTableName:
      Description: "DynamoDB Table Name"
      Value: !Ref mahnoorTempHvacTable

    ApiGatewayUrl:
      Description: "Dashboard API Base URL"
      Value:
        Fn::Sub: "https://${ApiGatewayRestApi}.execute-api.us-east-2.amazonaws.com/${self:provider.stage}"

    MqttFxThingName:
      Description: "IoT Thing name for MQTT.fx — use as Client ID"
      Value: !Ref mahnoorMqttFxThing

    MqttFxCertificateArn:
      Description: "Fetch cert/key from SSM: /mahnoor/mqttfx/certificate-pem and /mahnoor/mqttfx/private-key"
      Value: !GetAtt mahnoorMqttFxCertificate.CertificateArn

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: true
    slim: true
package:
  individually: false
  patterns:
    - '!**'
    - 'ingest_hvac.py'
    - 'process_hvac.py'
    - 'dashboard_api.py'
    - 'mqttfx_provisioner.py'
    - '!venv/**'
    - '!.venv/**'
    - '!node_modules/**'
    - '!functions/**'
    - '!__pycache__/**'
    - '!.serverless/**'
